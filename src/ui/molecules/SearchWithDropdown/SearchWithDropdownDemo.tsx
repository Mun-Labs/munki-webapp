import { SearchWithDropdown } from "./SearchWithDropdown";

export const SearchWithDropdownDemo = () => {
  return (
    <div style={{ width: 300, height: 100 }}>
      <SearchWithDropdown></SearchWithDropdown>
    </div>
  );
};
